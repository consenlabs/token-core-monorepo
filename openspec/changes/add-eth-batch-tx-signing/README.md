# add-eth-batch-tx-signing

Add batch ETH transaction signing in token-core (tcx) and imkey-core (ikc) so a single SDK call can sign multiple Ethereum transactions (e.g. prepay + stake) instead of forcing two round-trips.
